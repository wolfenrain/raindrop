# Required: the driver package the CLI introspects and generates with.
driver: raindrop_postgres

# Optional schemas path, used to generate migrations.
schemas: lib/schemas

# Optional output path for the generated migrations.
# out: migrations/
