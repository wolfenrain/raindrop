name: raindrop_postgres_example
publish_to: none
resolution: workspace

environment:
  sdk: ^3.6.0

dependencies:
  postgres: ^3.5.0
  raindrop:
    path: ../../raindrop
  raindrop_postgres:
    path: ../

dev_dependencies:
  raindrop_cli:
    path: ../../raindrop_cli