name: raindrop_cli
description: CLI tool for generating SQL migrations from Raindrop schema definitions.
version: 0.1.0
publish_to: none
resolution: workspace

environment:
  sdk: ">3.5.0<=4.0.0"

dependencies:
  raindrop: ^0.1.0
  args: ^2.4.0
  yaml: ^3.1.0
  analyzer: ^8.0.0
  path: ^1.8.0
  crypto: ^3.0.7

dev_dependencies:
  test: ^1.24.0
  lints: ^5.1.1

executables:
  raindrop: raindrop
